/**
 * @param {string} s
 * @return {string}
 */
var smallestString = function (s) {
  s = s.split('');
  let i = 0;
  while (i < s.length) {
    if (s[i] != 'a') {
      let start = i;
    }
    i++;
  }
};
