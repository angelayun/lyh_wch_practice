/**
 * @param {number[][]} grid
 * @return {number[]}
 */
var zigzagTraversal = function (grid) {
  let rows = grid.length,
    cols = grid[0].length;
  let i = 0,
    j = 0;
  let flag = true;
  while (i < rows && j < cols) {
    if (flag) {
      i += 2;
      if(i>)
    } else {
      i -= 2;
    }
  }
};
