/**
 * @param {number[]} dist
 * @param {number} hour
 * @return {number}
 */
var minSpeedOnTime = function (dist, hour) {
  let total = 0
  hour = Math.ceil(hour)
  for(let d of dist){
    let time = Math.ceil()
  }
};