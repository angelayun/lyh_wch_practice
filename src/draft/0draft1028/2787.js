/**
 * @param {number} n
 * @param {number} x
 * @return {number}
 */
var numberOfWays = function (n, x) {
  const MX_N = 300,
    MX_X = 5;
  const f = Array.from({ length: MX_N + 1 }, () => new Array(MX_X).fill(0));
  for (let x = 1; x <= MX_X; x++){
    
  }
};
