/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var maxFrequency = function (nums, k) {
  // 从小到大排序
  nums.sort((a, b) => a - b);
  // 滑入窗口消耗的次数
  let winDiss = 0
  const n = nums.length
  for (let left = 0, right = 0; right < n; right++){
    // TODO 写不下去了
  }
  
};
