/**
 * @param {number[]} nums
 * @return {number}
 */
var maxGoodNumber = function (nums) {
  nums.sort((a, b) => {
    let c = a.toString(2);
    let d = b.toString(2);
    // c-d
    // 从左往右比  谁的位上是1 而别的位上是0的  则这个排前面
    
  });
};
